name: GObject Lint

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    permissions:
      security-events: write  # Required for uploading SARIF results

    steps:
      - uses: actions/checkout@v6

      - name: Install gobject-lint
        run: |
          cargo install --git https://github.com/bilelmoussaoui/gobject-lint --branch sarif gobject-lint

      - name: Run gobject-lint
        run: |
          gobject-lint --format sarif > gobject-lint.sarif
        continue-on-error: true  # Don't fail the workflow on lint errors

      - name: Upload SARIF as artifact
        uses: actions/upload-artifact@v4
        with:
          name: gobject-lint-sarif
          path: gobject-lint.sarif

      - name: Upload SARIF results
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: gobject-lint.sarif
          category: gobject-lint
